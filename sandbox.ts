// arrays
let names = ['luigi', 'mario', 'yoshi'];
names.push('toad');

let numbers = [10, 20, 30, 40];
numbers.push(25);

let mixed = ['ken', 4, 'chun', 94, true];
mixed.push('ra');
mixed.push(10);

// objects
let ninja = {
  name: 'mario',
  belt: 'black',
  age: 30
}

ninja.age = 40;
ninja.name = 'ryu';

ninja = {
  name: 'yoshi',
  belt: 'orange',
  // age: 50
}
